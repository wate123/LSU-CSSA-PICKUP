/*
 * BeVolunteer Messages
 *
 * This contains all the text for the BeVolunteer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BeVolunteer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BeVolunteer container!',
  },
});
