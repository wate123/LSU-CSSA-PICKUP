/*
 * RequestStatus Messages
 *
 * This contains all the text for the RequestStatus container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestStatus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestStatus container!',
  },
});
