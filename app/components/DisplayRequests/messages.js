/*
 * DisplayRequests Messages
 *
 * This contains all the text for the DisplayRequests component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DisplayRequests';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DisplayRequests component!',
  },
});
