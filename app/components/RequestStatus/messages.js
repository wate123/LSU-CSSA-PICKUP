/*
 * RequestStatusForm Messages
 *
 * This contains all the text for the RequestStatusForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RequestStatusForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestStatusForm component!',
  },
});
